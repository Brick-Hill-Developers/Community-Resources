// script made by Rares :)
let bot = new Bot("Name")
  bot.setAvatar() // set the avatar of the bot ( this is only working with real player's outfits )
  Game.newBot(bot)
  bot.setSpeech("Text") // the thing that you want the bot to say
  bot.setPosition(new Vector3(-13,16,0)) // the position
  bot.setRotation(new Vector3(0,0,90)) // the rotation
